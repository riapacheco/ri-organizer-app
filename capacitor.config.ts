import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.riorganizer.app',
  appName: 'ri-organizer-app',
  webDir: 'dist/ri-organizer-app',
  bundledWebRuntime: false
};

export default config;
